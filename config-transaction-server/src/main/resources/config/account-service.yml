server:
  port: 8081

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
      
management:
  tracing:
    sampling:
      probability: 1.0
spring:
  #JDBC
  datasource:
    url: jdbc:mysql://localhost:3306/transactionProcessingSystem
    username: root
    password: Balajisai@1234#
  #hibernate (JPA)
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect